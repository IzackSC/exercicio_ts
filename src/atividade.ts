// Multiplicação //

const numero1: number = 5;
const numero2: number = 10;

function multiplicaNumero(numero1: number, numero2: number): number {
    return numero1 * numero2;
}

// Função de saudação //

const nome: string = 'Izack';

function saudacao(nome: string): string {
    return 'Olá' + nome;
    // return `Olá ${nome}`
}
